name: "e_stat"
version: "1.0.0"

profile: "e_stat"

model-paths: ["models"]
macro-paths: ["macros", "shared/macros"]
clean-targets: ["target", "dbt_packages"]

seeds:
  e_stat:
    +database: e_stat
    +schema: main

models:
  e_stat:
    +database: e_stat
    +materialized: view
    boundary:
      raw:
        +materialized: table
      stg:
        +materialized: table
    census:
      raw:
        +materialized: table
      stg:
        +materialized: table
    code:
      raw:
        +materialized: table
      stg:
        +materialized: table
    economic_census:
      raw:
        +materialized: table
      stg:
        +materialized: table
    manufacture:
      raw:
        +materialized: table
      stg:
        +materialized: table
    resident_registry:
      raw:
        +materialized: table
      stg:
        +materialized: table
